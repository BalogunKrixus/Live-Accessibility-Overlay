{
  "name": "live-accessibility-overlay",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension that surfaces accessibility problems on the live page, in context.",
  "type": "module",
  "scripts": {
    "test": "npm run lint && npm run test:unit && npm run test:ext",
    "test:unit": "node test/harness.mjs",
    "test:ext": "node test/extension.mjs",
    "shots": "node test/harness.mjs --shots",
    "icons": "node tools/make-icons.mjs",
    "lint": "node tools/check-syntax.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.0"
  }
}
